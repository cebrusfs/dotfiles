#!/bin/bash
# vim: syntax=bash:
#
# ============================================================================
# ROLE & GOAL:
# This is the "Full Sync & Bootstrap" script.
# Its purpose is to perform a heavy, declarative synchronization of the system
# state against the configuration files. It handles everything from creating
# symlinks, installing OS packages, to installing all missing dev tools via `mise`
# and PRUNING unused tools. It should be run when setting up a new environment
# or when applying structural changes to the dotfiles.
# ============================================================================

set -e

BASEDIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
# shellcheck source=/dev/null
source "${BASEDIR}/install-conf/utils.sh"

DOTBOT_DIR="modules/dotbot"

function dotbot_install() {
    CONFIG="install-conf/dotbot.conf.yaml"
    DOTBOT_BIN="bin/dotbot"

    log "Install with dotbot config"
    "${BASEDIR}/${DOTBOT_DIR}/${DOTBOT_BIN}" -d "${BASEDIR}" -c "${CONFIG}" "${@}"
    echo ""
}

# Initialize and sync dotbot git submodules
function dotbot_init() {
    log "Initialize dotbot module"
    git -C "${DOTBOT_DIR}" submodule sync --quiet --recursive
    git submodule update --init --recursive "${DOTBOT_DIR}"
    echo ""
}

# Phase 1: Set up symlinks using Dotbot
function dotbot_main() {
    dotbot_init
    dotbot_install "$@"
}

# Phase 2: Install system-level dependencies (Homebrew for macOS, APT/DNF for Linux)
function os_packages_main() {
    if is_osx; then
        # shellcheck source=/dev/null
        . install-conf/homebrew-install.sh
        # Make sure brew is in PATH for later steps
        if [[ -f "/opt/homebrew/bin/brew" ]]; then
            eval "$(/opt/homebrew/bin/brew shellenv)"
        fi
    elif is_linux; then
        linux_install zsh
        linux_install vim
    else
        error "Unknown OSTYPE: $OSTYPE"
    fi
}

# Phase 3: Bootstrap `mise` and install/prune development tools
# This step ensures the exact toolset declared in config.toml is installed.
function dev_tools_main() {
    if ! check_command mise; then
        log "Installing mise..."
        if is_osx; then
            brew install mise
        elif is_linux; then
            MISE_QUIET=1 curl -s https://mise.run | sh
            # Ensure it is in PATH for this script session
            export PATH="$HOME/.local/bin:$PATH"
        fi
    fi

    if check_command mise; then
        # Activate mise to ensure its shims are in PATH and can be used immediately
        eval "$(mise activate bash)"

        # Bootstrap bun first so mise can use it for npm: packages
        log "Bootstrapping bun via mise..."
        mise install bun -y

        # At this point, Phase 1 has already symlinked ~/.config/mise/config*.toml
        # So mise will read the correct global config when running install
        log "Running 'mise install' to fetch the remaining CLI tools..."
        mise install -y

        log "Pruning unused mise tools..."
        mise prune -y
    else
        error "mise not found! Installation failed."
    fi
}

# Phase 4: Install and update Vim/Neovim plugins
function vim_plugins_main() {
    log "Install VIM plugins"
    # Plugins update (Mason, Plug, etc)
    # shellcheck source=/dev/null
    . config/vim/update
}

# Phase 5: Ensure Zsh is the default shell
function zsh_setup_main() {
    if [[ "$SHELL" != *zsh ]]; then
        log "Current shell is not zsh, switch default shell to zsh"
        chsh -s "$(which zsh)"
    fi
}

function main() {
    umask 022

    pushd "${BASEDIR}"

    print_stage "Phase 1: Create Symlinks (Dotbot)"
    dotbot_main "$@"

    print_stage "Phase 2: Install OS dependencies (OS Packages)"
    os_packages_main

    print_stage "Phase 3: Install development tools (Dev Tools)"
    dev_tools_main
    apply_codex_config

    print_stage "Phase 4: Install Vim/Neovim plugins"
    vim_plugins_main

    print_stage "Phase 5: Set Zsh as default shell"
    zsh_setup_main

    print_stage "Installation Completed Successfully! 🎉"

    popd >/dev/null

    # Switch shell at the very last step to replace the old behavior that caused script restarting mid-way
    if [[ "$SHELL" != *zsh ]]; then
        log "Restarting shell to apply changes..."
        exec zsh -l
    fi
}

main "$@"
