# Mac only

# Note: tmux 2.6+ don't need this wrapper
# Support clipboard under tmux
# https://github.com/ChrisJohnsen/tmux-MacOSX-pasteboard
# set-option -g default-command "reattach-to-user-namespace -l zsh || zsh"
