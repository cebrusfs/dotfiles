interface:
  display_name: "GitHub Issues"
  short_description: "Manage GitHub issues with gh"
  default_prompt: "Use $gh-cli to manage GitHub issues and dependencies. Default to --body-file for issue, PR, review, and comment bodies; use --body only for short single-line plain text with no special characters."
