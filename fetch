# vim: filetype=sh

git clone https://github.com/cebrusfs/dotfiles.git "$HOME/.dotfiles"
cd "$HOME/.dotfiles"

# Set upstream push
git remote set-url --push origin git@github.com:cebrusfs/dotfiles.git

. ./install
